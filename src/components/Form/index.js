import React from 'react';

import './style.scss';

const Form = () => (
  <form>
    <input
      type="text"
      placeholder="Saisissez votre message..."
    />
    <button type="button">&gt;</button>
  </form>
);

export default Form;
